# ==============================================================================
# CONFIGURAÇÃO PADRÃO PARA MELHORIAS DE DESEMPENHO, SEGURANÇA E SEO
# ==============================================================================

# Defina a página de índice padrão do diretório.
# Esta era a sua configuração original e será mantida.
DirectoryIndex login.php

# ------------------------------------------------------------------------------
# MELHORIAS DE DESEMPENHO
# ------------------------------------------------------------------------------

# Ativa a compressão GZIP para carregar o site mais rápido.
# Isso reduz o tamanho de arquivos de texto (HTML, CSS, JS) antes de enviá-los
# ao navegador do visitante.
<IfModule mod_deflate.c>
  AddOutputFilterByType DEFLATE text/plain
  AddOutputFilterByType DEFLATE text/html
  AddOutputFilterByType DEFLATE text/xml
  AddOutputFilterByType DEFLATE text/css
  AddOutputFilterByType DEFLATE application/xml
  AddOutputFilterByType DEFLATE application/xhtml+xml
  AddOutputFilterByType DEFLATE application/rss+xml
  AddOutputFilterByType DEFLATE application/javascript
  AddOutputFilterByType DEFLATE application/x-javascript
</IfModule>

# Define por quanto tempo os arquivos devem ser armazenados no cache do navegador.
# Isso faz com que visitantes recorrentes carreguem seu site muito mais rápido,
# pois não precisarão baixar novamente arquivos como imagens, CSS e JS.
<IfModule mod_expires.c>
  ExpiresActive On
  ExpiresByType image/jpg "access plus 1 year"
  ExpiresByType image/jpeg "access plus 1 year"
  ExpiresByType image/gif "access plus 1 year"
  ExpiresByType image/png "access plus 1 year"
  ExpiresByType image/svg+xml "access plus 1 year"
  ExpiresByType image/webp "access plus 1 year"
  ExpiresByType text/css "access plus 1 month"
  ExpiresByType application/javascript "access plus 1 month"
  ExpiresByType application/pdf "access plus 1 month"
  ExpiresByType image/x-icon "access plus 1 year"
  ExpiresDefault "access plus 2 days"
</IfModule>

# ------------------------------------------------------------------------------
# MELHORIAS DE SEGURANÇA
# ------------------------------------------------------------------------------

# Desativa a listagem de arquivos em diretórios.
# Se um diretório não tiver um arquivo de índice (como o seu login.php),
# o servidor não mostrará uma lista de todos os arquivos, o que é mais seguro.
Options -Indexes

# Protege o próprio arquivo .htaccess contra acesso público.
# Ninguém poderá ver o conteúdo deste arquivo pelo navegador.
<Files ".htaccess">
  Require all denied
</Files>

# Protege outros arquivos de configuração sensíveis (exemplo: wp-config.php do WordPress)
# Você pode adicionar outras regras <Files> para arquivos que não devem ser acessados.
# <Files "outro_arquivo_sensivel.php">
#  Require all denied
# </Files>

# ------------------------------------------------------------------------------
# MELHORIAS DE SEO (URLS AMIGÁVEIS E REDIRECIONAMENTOS)
# ------------------------------------------------------------------------------

# Ativa o motor de reescrita de URL, necessário para as regras abaixo.
RewriteEngine On

# Força o uso de HTTPS (conexão segura).
# Se você tem um certificado SSL instalado, esta regra redireciona todo o tráfego
# de http:// para https://. Descomente as duas linhas abaixo se tiver SSL.
# ATENÇÃO: Só ative se o seu site funciona corretamente com https://
# RewriteCond %{HTTPS} off
# RewriteRule ^(.*)$ https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301]

# Redireciona de "sem www" para "com www".
# Isso é bom para SEO, pois evita conteúdo duplicado. Escolha uma versão e mantenha.
# Altere "seusite.com.br" para o seu domínio.
# Descomente as duas linhas abaixo para ativar.
# RewriteCond %{HTTP_HOST} ^seusite.com.br [NC]
# RewriteRule ^(.*)$ http://www.seusite.com.br/$1 [L,R=301,NC]

# --- OU ---

# Redireciona de "com www" para "sem www" (mais comum hoje em dia).
# Altere "seusite.com.br" para o seu domínio.
# Descomente as duas linhas abaixo para ativar. Lembre-se de escolher apenas UMA das opções.
# RewriteCond %{HTTP_HOST} ^www.seusite.com.br [NC]
# RewriteRule ^(.*)$ http://seusite.com.br/$1 [L,R=301,NC]

# ==============================================================================
# FIM DA CONFIGURAÇÃO
# ==============================================================================